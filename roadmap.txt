================================================================================
RADIO & TV STORY SEGMENTER - DEVELOPMENT ROADMAP & TECHNICAL SPECIFICATION
================================================================================
This document outlines the architectural roadmap and implementation specifications
for future versions of the Radio & TV Story Segmenter. Adapted from best-in-class
heuristics, modular runtime architectures, and timeline serialization algorithms.

--------------------------------------------------------------------------------
ROADMAP MAINTENANCE POLICY: N-1 SLIDING-WINDOW & STATUS TRACKING
--------------------------------------------------------------------------------
- The roadmap maintains a rolling window: it displays the current/immediate past
  release ($N-1$) for baseline context, followed by active upcoming development
  milestones ($N$, $N+1$).
- Status Notation Policy (Modeled after Social Digest):
  * All milestones and feature items are annotated with explicit progress indicators:
    - [x] Completed item / feature
    - [-] In-progress item / active development
    - [ ] Planned / not started item
  * When work commences on an item, mark it immediately as [-] [IN-PROGRESS].
  * Mark an item as [x] [COMPLETED] when implementation is finalized, upon user
    confirmation, or inferred when development transitions to another part of the roadmap.
- When a new version is built, its completed specifications are recorded, and any
  versions older than $N-1$ are pruned to keep the document focused and actionable.
- Whenever any code change, bug fix, packaging adjustment, or feature refinement is
  made during a release cycle, it MUST be recorded immediately in the current release
  section of roadmap.txt and CHANGELOG.md (even if it occurs after the initial version
  number bump).

--------------------------------------------------------------------------------
TABLE OF CONTENTS & STATUS OVERVIEW
--------------------------------------------------------------------------------
1. Previous Release: QProcess Buffer Draining & Speaker Detection Progress Detail (Completed in v3.3.24)
   [x] 1.1 Added `read_output()` at the beginning of the `finished` slot in `translation.py` to fully drain and process remaining standard output buffer.
   [x] 1.2 Swapped hardcoded progress message with the dynamic helper message in `processing.py` to show precise diarization steps in real-time.

2. Current Release: QProcess Buffer Flush & Truncation Hotfix (Completed in v3.3.25)
   [x] 2.1 Enforced `force_flush=True` at process exit in `processing.py` and `translation.py` to parse leftover JSON without trailing newlines.
   [x] 2.2 Resolved the "Translation complete. (100%)" application hang caused by orphaned pipes.

3. Future Milestone: Expanded NLE & DAW Timeline Interchange (Phase 4 - Planned for v3.4) [NOT STARTED]
   [ ] 3.1 Cockos Reaper Project (.rpp) Generator
   [ ] 3.2 Magix Samplitude EDL (v1.5) Export
   [ ] 3.3 Video NLE Interchange (FCPXML, Premiere / DaVinci Resolve xmeml XML)
   [ ] 3.4 Avid / Pro Tools AAF Composition
   [ ] 3.5 Rational Timecode & Audio Sample Rate Math
   [ ] 3.6 UI & Batch Export Integration

4. Future Milestone: Text-Driven Audio Editing & Precision Alignment (Phase 5 - Planned for v4.0) [NOT STARTED]
   [ ] 4.1 "Text-as-Timeline" Rough-Cut Editing Engine
   [ ] 4.2 Seamless Non-Destructive Playback Preview
   [ ] 4.3 CTC Word Boundary Refinement & Forced Alignment
   [ ] 4.4 Memory-Bounded Diarization & Chunked Streaming

================================================================================
CORE ARCHITECTURAL INVARIANTS
================================================================================
- Core modules must NEVER import from plugins/.
- Heavy translation runtimes must remain isolated inside plugins/translation/.
- Speaker diarization uses wespeakerruntime as the primary ONNX embedding engine.
- Real-time progress updates with percentage, status messages, and estimated time.
- Diagnostic logs written to OS standard logging locations.
- Offline-first architecture: core processing functions locally without internet.
- 0 MB Default GPU Footprint: Base installer contains 0 MB of CUDA bloat; GPU features are purely optional and provisioned on demand.

================================================================================
================================================================================
1. PREVIOUS RELEASE: QPROCESS BUFFER DRAINING & SPEAKER DETECT PROGRESS (Completed in v3.3.24)
================================================================================

[x] 1.1 RESOLVED QPROCESS STDOUT RACE CONDITION ON EXIT
-------------------------------------------------------
- Discovered that when the translation subprocess finishes and exits, the final `"finished"` JSON message is sometimes left unread in the QProcess stdout buffer because the `finished` slot did not read standard output before unregistering.
- Explicitly called `read_output()` inside the `finished` slot in `translation.py` to safely drain and parse any remaining payload data, enabling instant, reliable completion.

[x] 1.2 IMPLEMENTED DYNAMIC SPEAKER DETECT PROGRESS DETAIL
----------------------------------------------------------
- Replaced the generic hardcoded progress message placeholder with the actual, dynamic subprocess status message in `processing.py`.
- Keeps the user informed of active background steps (such as VAD, signature extraction, and AHC clustering) in real-time, eliminating the perception of being stuck.

================================================================================
2. PREVIOUS RELEASE: QPROCESS BUFFER FLUSH & TRUNCATION HOTFIX (Completed in v3.3.25)
================================================================================

[x] 2.1 ENFORCED BUFFER FLUSH ON MISSING TRAILING NEWLINES
----------------------------------------------------------
- Fixed a bug where a final JSON payload missing a trailing newline would be left orphaned in the output buffer when `QProcess` closed its pipes.
- Updated `processing.py` and `translation.py` to aggressively parse remaining chunks via `force_flush=True`.

================================================================================
3. CURRENT RELEASE: SILENT SUBPROCESS PAYLOAD DROP & UNICODE FIX (Completed in v3.3.26)
================================================================================

[x] 3.1 FIXED UNICODE ENCODE ERRORS IN SUBPROCESS STDOUT
--------------------------------------------------------
- Found the actual root cause of the "Translation complete (100%)" application hang. 
- `sys.stdout.write` on Windows was defaulting to `cp1252`, causing unencodable characters (e.g. Spanish accents) to throw a `UnicodeEncodeError`. 
- The background thread caught and discarded this exception silently, causing the process to exit with code 0 but drop the final translated payload entirely.
- Transitioned all `emit()` IPC functions in `plugins/translation/runtime_entry.py` and `radio_tv_story_segmenter_worker.py` to write raw UTF-8 bytes to `sys.stdout.buffer` directly.
================================================================================

[x] 2.1 ENFORCED BUFFER FLUSH ON MISSING TRAILING NEWLINES
----------------------------------------------------------
- Fixed a bug where a final JSON payload missing a trailing newline would be left orphaned in the output buffer when `QProcess` closed its pipes.
- Updated `processing.py` and `translation.py` to aggressively parse remaining chunks via `force_flush=True`.

================================================================================
4. FUTURE MILESTONE: EXPANDED NLE & DAW TIMELINE INTERCHANGE (Phase 4 - Planned for v3.4) [NOT STARTED]
================================================================================

- Generates non-destructive timeline project files to import translated or diarized stories seamlessly into professional post-production workflows.
- 3.1 Cockos Reaper (.rpp) Generator
- 3.2 Magix Samplitude EDL (.edl)
- 3.3 Video NLE Interchange (FCPXML / Premiere Pro)
- 3.4 Avid / Pro Tools AAF Composition

================================================================================
5. FUTURE MILESTONE: TEXT-DRIVEN AUDIO EDITING & PRECISION ALIGNMENT (Phase 5 - Planned for v4.0) [NOT STARTED]
================================================================================

- "Text-as-Timeline" Rough-Cut Editing Engine
- Seamless Non-Destructive Playback Preview
- CTC Word Boundary Refinement & Forced Alignment
- Memory-Bounded Diarization & Chunked Streaming
