================================================================================
RADIO & TV STORY SEGMENTER - DEVELOPMENT ROADMAP & TECHNICAL SPECIFICATION
================================================================================
This document outlines the architectural roadmap and implementation specifications
for future versions of the Radio & TV Story Segmenter. Adapted from best-in-class
heuristics, modular runtime architectures, and timeline serialization algorithms.

--------------------------------------------------------------------------------
ROADMAP MAINTENANCE POLICY: N-1 SLIDING-WINDOW & STATUS TRACKING
--------------------------------------------------------------------------------
- The roadmap maintains a rolling window: it displays the current/immediate past
  release ($N-1$) for baseline context, followed by active upcoming development
  milestones ($N$, $N+1$).
- Status Notation Policy (Modeled after Social Digest):
  * All milestones and feature items are annotated with explicit progress indicators:
    - [x] Completed item / feature
    - [-] In-progress item / active development
    - [ ] Planned / not started item
  * When work commences on an item, mark it immediately as [-] [IN-PROGRESS].
  * Mark an item as [x] [COMPLETED] when implementation is finalized, upon user
    confirmation, or inferred when development transitions to another part of the roadmap.
- When a new version is built, its completed specifications are recorded, and any
  versions older than $N-1$ are pruned to keep the document focused and actionable.
- Whenever any code change, bug fix, packaging adjustment, or feature refinement is
  made during a release cycle, it MUST be recorded immediately in the current release
  section of roadmap.txt and CHANGELOG.md (even if it occurs after the initial version
  number bump).

--------------------------------------------------------------------------------
TABLE OF CONTENTS & STATUS OVERVIEW
--------------------------------------------------------------------------------
1. Previous Release: Optional Fade Defaults & Audio/Timeline Performance Optimization (Completed in v3.3.3)
   [x] 1.1 Timeline Fades Optional by Default (`enable_audio_fades = false` and `preview_audio_fades = false` on new installs across all init paths, QSettings fallbacks, and reset defaults)
   [x] 1.2 Audio Output Volume Throttling & Playback Cache (Eliminated redundant `setVolume()` calls into audio daemon via `_last_applied_fade_vol` threshold caching; 35ms timer interval)
   [x] 1.3 Mouse Hover & Tooltip Lag Elimination (Suppressed 100 Hz millisecond tooltip thrashing across timeline canvas; isolated tooltips strictly to interactive story boundaries and fade grab handles)
   [x] 1.4 Optimized Fade Overlay & Vector Painting (Replaced antialiased dashed line calculations with high-efficiency solid vector ramp strokes and clamped polygon vertices)

2. Current Release: Non-Linear Fade Curves, Magnetic Snapping, & Context Menu Audio Fades (Completed in v3.3.4)
   [x] 2.1 Non-Linear Audio Fade Envelope Curves (Linear, Cosine S-Curve, Logarithmic, Exponential gain math, vector rendering, and QSettings defaults)
   [x] 2.2 Magnetic Timeline Snapping Engine (`snap_time()` magnetically snaps selection region drag, playhead scrubbing, story edge trimming, and fade handles within 10px threshold)
   [x] 2.3 Story List Context Menu & Batch Audio Fade Controls (`StoryListWidget` right-click context menu with dedicated Audio Fades sub-menu for auditioning, batch apply/remove, and curve profile selection)

3. Future Milestone: Expanded NLE & DAW Timeline Interchange (Phase 4 - Planned for v3.4) [NOT STARTED]
   [ ] 3.1 Cockos Reaper Project (.rpp) Generator
   [ ] 3.2 Magix Samplitude EDL (v1.5) Export
   [ ] 3.3 Video NLE Interchange (FCPXML, Premiere / DaVinci Resolve xmeml XML)
   [ ] 3.4 Avid / Pro Tools AAF Composition
   [ ] 3.5 Rational Timecode & Audio Sample Rate Math
   [ ] 3.6 UI & Batch Export Integration

4. Future Milestone: Text-Driven Audio Editing & Precision Alignment (Phase 5 - Planned for v4.0) [NOT STARTED]
   [ ] 4.1 "Text-as-Timeline" Rough-Cut Editing Engine
   [ ] 4.2 Seamless Non-Destructive Playback Preview
   [ ] 4.3 CTC Word Boundary Refinement & Forced Alignment
   [ ] 4.4 Memory-Bounded Diarization & Chunked Streaming

================================================================================
CORE ARCHITECTURAL INVARIANTS
================================================================================
- Core modules must NEVER import from plugins/.
- Heavy translation runtimes must remain isolated inside plugins/translation/.
- Speaker diarization uses wespeakerruntime as the primary ONNX embedding engine.
- Real-time progress updates with percentage, status messages, and estimated time.
- Diagnostic logs written to OS standard logging locations.
- Offline-first architecture: core processing functions locally without internet.
- 0 MB Default GPU Footprint: Base installer contains 0 MB of CUDA bloat; GPU features are purely optional and provisioned on demand.

================================================================================
1. PREVIOUS RELEASE: OPTIONAL FADE DEFAULTS & AUDIO/TIMELINE PERFORMANCE OPTIMIZATION (Completed in v3.3.3)
================================================================================

[x] 1.1 TIMELINE FADES OPTIONAL BY DEFAULT ON NEW INSTALLS
----------------------------------------------------------
- Configured `enable_audio_fades` and `preview_audio_fades` to default to `False` across all application initialization paths, QSettings fallbacks, and the "Restore Defaults" preferences routine.
- New installations keep timeline audio fades cleanly disabled until explicitly enabled by the user in Preferences -> *"Playback & Timeline"*, preserving maximum interface responsiveness and visual simplicity.
- Existing user configurations that have already enabled or disabled the feature are respected and preserved across sessions.

[x] 1.2 AUDIO OUTPUT VOLUME THROTTLING & PLAYBACK PERFORMANCE OPTIMIZATION
--------------------------------------------------------------------------
- Eliminated high-frequency redundant `setVolume()` calls into the system audio daemon during playback by tracking `_last_applied_fade_vol` and only applying gain changes when volume shifts by `>= 0.005`.
- Adjusted real-time fade preview timer interval to 35ms (~28 FPS), significantly reducing CPU overhead while maintaining smooth, artifact-free audio crossfades.
- Automatically suspends fade timers and restores full master volume when playback pauses or reaches the end of an auditioned story segment.

[x] 1.3 TIMELINE CANVAS TOOLTIP STORM & MOUSE LAG ELIMINATION
-------------------------------------------------------------
- Fixed an issue where millisecond-precision timestamp recalculations caused `QToolTip.showText()` to fire continuously (up to 100 times per second) during any cursor movement across the timeline.
- Isolated timeline tooltips strictly to interactive hover targets (story edge boundaries and tactile fade envelope grab handles), automatically hiding tooltips during free scrubbing and panning for seamless 60+ FPS cursor response.
- Cached boundary and fade hit-test lookups within `mouseMoveEvent` to prevent redundant coordinate recalculations.

[x] 1.4 OPTIMIZED FADE OVERLAY & VECTOR PAINTING
------------------------------------------------
- Replaced computationally intensive antialiased dashed line calculations (`DashLine`) in `paintEvent` with crisp, high-performance solid 1.2px vector ramp strokes.
- Clamped polygon vertex arrays strictly to viewport bounds to minimize rasterization overhead.

================================================================================
2. CURRENT RELEASE: NON-LINEAR FADE CURVES, MAGNETIC SNAPPING, & CONTEXT MENU AUDIO FADES (Completed in v3.3.4)
================================================================================

[x] 2.1 NON-LINEAR AUDIO FADE ENVELOPE CURVES & CUSTOM GAIN PROFILES
---------------------------------------------------------------------
- Added selectable audio fade curve profiles: **Linear Ramp**, **Cosine S-Curve** ($0.5 \cdot (1 - \cos(\pi \cdot u))$), **Logarithmic** ($\log_{10}(1 + 9u)$), and **Exponential** ($(10^u - 1)/9$).
- Supported across real-time playback gain modulation (`get_fade_volume_factor_at_time`), interactive 16-step vector curve drawing on `TimelineCanvas`, global default setting in Preferences -> *"Playback & Timeline"*, and story JSON serialization (`fade_curve` field).

[x] 2.2 MAGNETIC TIMELINE SNAPPING ENGINE
-----------------------------------------
- Implemented magnetic snapping on `TimelineCanvas` (`snap_time()`) that magnetically snaps cursor drags during selection range adjustments, playhead scrubbing, story edge trimming, and audio fade handle dragging.
- Snaps to nearby story start/end boundaries, playhead location, and selection region anchors within a 10-pixel threshold (`enable_magnetic_snapping`).

[x] 2.3 STORY LIST CONTEXT MENU & BATCH AUDIO FADE CONTROLS
------------------------------------------------------------
- Created `StoryListWidget` right-click context menu with dedicated **Audio Fades** sub-menu allowing users to audition stories, batch apply default/custom fades, batch remove fades, and set fade curve profiles across selected stories.
- Expanded `StoryFadesDialog` to include fade curve profile selection and updated story list delegates with status tooltips and badges (`[In:0.5s Out:1.0s]`).

================================================================================
3. FUTURE MILESTONE: EXPANDED NLE & DAW TIMELINE INTERCHANGE (Phase 4 - Planned for v3.4) [NOT STARTED]
================================================================================

[ ] 3.1 COCKOS REAPER PROJECT (.rpp) GENERATOR
-----------------------------------------------
Upgrades the simple EDL marker export to full multi-track Reaper project files.
- RPP Structure: Root `<REAPER_PROJECT 0.1 "7.0">`, `<TRACK`, `<ITEM` definitions with micro-fades.

[ ] 3.2 MAGIX SAMPLITUDE EDL (v1.5) EXPORT
-------------------------------------------
Sample-accurate text-based cut-list widely supported by legacy radio broadcast consoles and DAW tools.

[ ] 3.3 VIDEO NLE INTERCHANGE (FCPXML & XMEML XML)
---------------------------------------------------
Generates sequence timelines for video newsrooms and multi-cam podcast edits (FCPXML v1.9/1.10 and Adobe Premiere/Resolve XML).

[ ] 3.4 AVID & PRO TOOLS AAF COMPOSITION
-----------------------------------------
Support AAF composition export for high-end post-production facilities.

[ ] 3.5 RATIONAL TIMECODE & AUDIO SAMPLE RATE MATH
---------------------------------------------------
Rational time base conversions to ensure sub-millisecond sync across DAW and NLE workflows.

[ ] 3.6 UI & BATCH EXPORT INTEGRATION
--------------------------------------
Expand Export Menu in `RadioTVSegmenter.py` with direct DAW and NLE options.

================================================================================
4. FUTURE MILESTONE: TEXT-DRIVEN AUDIO EDITING & PRECISION ALIGNMENT (Phase 5 - Planned for v4.0) [NOT STARTED]
================================================================================

[ ] 4.1 "TEXT-AS-TIMELINE" ROUGH-CUT EDITING ENGINE
----------------------------------------------------
Allows journalists to edit audio/video directly by striking through or deleting words in the transcript UI.

[ ] 4.2 SEAMLESS NON-DESTRUCTIVE PLAYBACK PREVIEW
--------------------------------------------------
Custom audio playback handler in PySide6 / QMediaPlayer playing strictly inside keep ranges with anti-click crossfades.

[ ] 4.3 CTC WORD BOUNDARY REFINEMENT & FORCED ALIGNMENT
--------------------------------------------------------
Lightweight acoustic phone alignment pass for sub-10ms boundary precision.

[ ] 4.4 MEMORY-BOUNDED DIARIZATION & CHUNKED STREAMING
-------------------------------------------------------
Chunked streaming diarization to avoid out-of-memory errors on long broadcast recordings.
