================================================================================
RADIO & TV STORY SEGMENTER - DEVELOPMENT ROADMAP & TECHNICAL SPECIFICATION
================================================================================
This document outlines the architectural roadmap and implementation specifications
for future versions of the Radio & TV Story Segmenter. Adapted from best-in-class
heuristics, modular runtime architectures, and timeline serialization algorithms.

--------------------------------------------------------------------------------
ROADMAP MAINTENANCE POLICY: N-1 SLIDING-WINDOW & STATUS TRACKING
--------------------------------------------------------------------------------
- The roadmap maintains a rolling window: it displays the current/immediate past
  release ($N-1$) for baseline context, followed by active upcoming development
  milestones ($N$, $N+1$).
- Status Notation Policy (Modeled after Social Digest):
  * All milestones and feature items are annotated with explicit progress indicators:
    - [x] Completed item / feature
    - [-] In-progress item / active development
    - [ ] Planned / not started item
  * When work commences on an item, mark it immediately as [-] [IN-PROGRESS].
  * Mark an item as [x] [COMPLETED] when implementation is finalized, upon user
    confirmation, or inferred when development transitions to another part of the roadmap.
- When a new version is built, its completed specifications are recorded, and any
  versions older than $N-1$ are pruned to keep the document focused and actionable.
- Whenever any code change, bug fix, packaging adjustment, or feature refinement is
  made during a release cycle, it MUST be recorded immediately in the current release
  section of roadmap.txt and CHANGELOG.md (even if it occurs after the initial version
  number bump).

--------------------------------------------------------------------------------
TABLE OF CONTENTS & STATUS OVERVIEW
--------------------------------------------------------------------------------
1. Previous Release: Windows Update Supervisor & Installer Retention Fix (Completed in v3.4.13)
   [x] 1.1 Hardened PowerShell Supervisor Exit Loop: Upgraded PID wait supervisor in `launch_and_install` (`updater.py`) to an explicit `try { Get-Process -Id $pidToWait -ErrorAction Stop } catch { break }` exception loop, guaranteeing the supervisor blocks until `RadioTVSegmenter.exe` has completely exited before invoking UAC elevation.
   [x] 1.2 Post-Exit Lock Settlement Buffer: Added a 2-second safety delay (`Start-Sleep -Seconds 2`) after application process exit to guarantee Windows kernel file handle cleanup and DLL unloads complete before triggering the update setup executable.
   [x] 1.3 Inno Setup Process Termination Directive: Configured `CloseApplications=yes` and `CloseApplicationsFilter=*.exe` in `RadioTVStorySegmenter.iss` so setup runs cleanly signal and wait for active application instances.
   [x] 1.4 Multi-Package Installer Retention: Relaxed auto-cleanup policy to `max_to_keep=2` across update checks and post-downloads, preserving recent downloaded installers in `AppData/Local/RadioTVSegmenter/updates` rather than purging previous packages.

2. Current Release: Desktop Robustness, Non-Blocking IPC & Polish (Completed in v3.4.14)
   [x] 2.1 Windows Path Escaping Fix: Escaped single quotes in `Unblock-File` call inside `updater.py` (`escaped = str(path).replace("'", "''")`) to ensure reliable unblocking on paths with apostrophes.
   [x] 2.2 Non-Blocking Single-Instance IPC & Socket Retention: Migrated `waitForReadyRead(1000)` in `_handle_ipc_connection()` (`RadioTVSegmenter.py`) to asynchronous `readyRead` signal handling, stored incoming sockets in `window._pending_ipc_sockets` to prevent premature Python garbage collection, and attached `deleteLater` on disconnection.
   [x] 2.3 Timeline Scroll Lock Safeguard: Wrapped `self.is_internal_scrollbar_update` modifications in `try...finally` blocks in `TimelineWidget.update_scrollbar_from_canvas` and `update_canvas_from_scrollbar` to prevent lock freezes on unexpected layout interruptions.
   [x] 2.4 Subprocess Set Thread-Safety: Added `_SUBPROCESS_LOCK = threading.Lock()` guarding `_ACTIVE_SUBPROCESSES` mutations and iterations in `runtime_manager.py` (`register_process`, `unregister_process`, and `kill_all_subprocesses`) to prevent `RuntimeError: Set changed size during iteration` during multi-threaded teardowns.
   [x] 2.5 Prune Dead Code & Clean Duplicate Initializers: Removed unreferenced `setup_visual_fade_curve_combo()` in `prs_shared.py` and eliminated redundant duplicate attribute assignments in `WaveformWorker.__init__`.
   [x] 2.6 Sync Application Header Metadata: Refreshed legacy header docstrings in `RadioTVSegmenter.py` and `transcript_story.py` to match current project version.
   [x] 2.7 GitHub Actions CI/CD Hardening: Replaced fragile `setup-ffmpeg@v3` action with direct static Windows binary download (`Tyrrrz/FFmpegBin` v7.0.2) matching macOS, eliminating runner assertion errors (`Cannot get latest release` / `Requested version is not available`), and removed conflicting Node 20 environment overrides in `.github/workflows/build.yml`.

3. Upcoming Milestone: Automated Test Runner & Performance Benchmark Suite [PLANNED for v3.4.15]
   [ ] 3.1 Zero-Dependency Core Test Runner (`test_runner.py`):
       - Multi-tiered CLI test harness using Python's built-in `unittest` and Qt `QTest` with sandbox temporary directory isolation.
       - In-memory synthetic audio generation fixture (`wave`/`math`) producing realistic multi-tone audio without external files.
       - Tier 1: Pure logic unit tests (RTVS project file round-trip serialization, time string parsing/formatting fuzzing, safe filename sanitization, subtitle SRT/VTT generators, and WordPress export payload validation).
       - Tier 2: Media processing & worker concurrency tests (FFmpeg executable discovery, synthetic waveform envelope calculation, worker thread-safe cancellation, and `_SUBPROCESS_LOCK` stress tests).
       - Tier 3: Headless Qt UI & event loop integration (`QT_QPA_PLATFORM=offscreen` testing `InteractiveTranscriptEdit` scroll-lock stability, `TimelineWidget` scrollbar synchronization, and `QUndoStack` state restoration).
       - Tier 4: Environment & native ML runtime smoke test integration (delegating to `RadioTVSegmenter.py --self-test`).
   [ ] 3.2 Performance & Speed Benchmark Engine (`benchmark.py` / CLI & Diagnostic GUI):
       - Real-Time Factor (RTF) calculation engine ($\text{RTF} = \text{Audio Duration} / \text{Processing Time}$) with `time.perf_counter()` nanosecond precision.
       - Waveform audio decoding and envelope extraction throughput benchmark (MB/s & RTF).
       - Silero VAD chunk evaluation latency & WeSpeaker ONNX embedding extraction speed (embeddings/second).
       - Project state serialization / deserialization throughput benchmark (segments/second).
       - UI formatting & document reflow latency benchmark (words/second rendered).
       - Diagnostic report formatter with CPU/GPU hardware profile and optional JSON export.
       - (Optional GUI) "Run System Benchmark..." dialog under Help menu with progress bar and copyable summary.
   [ ] 3.3 Continuous Maintenance & Future Feature Test Coverage Policy:
       - Active policy requiring all future releases that introduce new processing features, export plugins, or UI widgets to synchronously add corresponding unit tests to `test_runner.py` and benchmark checkpoints to `benchmark.py`.

4. Future Milestone: Phase 2 Modularization of prs_shared.py (UI & Qt Components) [PLANNED]
   [ ] 4.1 Cache Management & ClearCacheDialog Extraction (`cache_manager.py`)
   [ ] 4.2 Story Widgets, Cards, & Metadata Dialogs (`story_widgets.py`)
   [ ] 4.3 Timeline Ruler, Track Views, & Playhead Overlays (`timeline_widgets.py`)
   [ ] 4.4 Transcript Viewer, Word Highlighting, & Search/Replace (`transcript_editor.py`)
   [ ] 4.5 Cautious Incremental Testing Protocol (Verified step-by-step testing after each extraction)

5. Future Milestone: Google Docs & Drive Export Plugin (`plugins/gdocs`) (Planned for v3.5) [NOT STARTED]
   [ ] 5.1 Plugin Architecture & OAuth 2.0 Authentication Engine (`plugins/gdocs/auth.py`, keyring token storage, local loopback handler)
   [ ] 5.2 Google Docs Document Serializer & REST Exporter (Formatted text, speaker headers, word highlights, story metadata)
   [ ] 5.3 Native Margin Comments & Text Range Anchoring (`drive.comments.create` integration for story notes & transcript comments)
   [ ] 5.4 Unified Export Center & Menu Integration (Progress worker, batch export, zero core bundle overhead)
   [ ] 5.5 (Optional) One-Time Review & Comment Pull ("Import Reviews from Google Doc" diff dialog)

6. Future Milestone: Expanded NLE & DAW Timeline Interchange (Phase 4 - Planned for v3.6) [NOT STARTED]
   [ ] 6.1 Cockos Reaper Project (.rpp) Generator
   [ ] 6.2 Magix Samplitude EDL (v1.5) Export
   [ ] 6.3 Video NLE Interchange (FCPXML, Premiere / DaVinci Resolve xmeml XML)
   [ ] 6.4 Avid / Pro Tools AAF Composition
   [ ] 6.5 Rational Timecode & Audio Sample Rate Math
   [ ] 6.6 UI & Batch Export Integration

7. Future Milestone: Text-Driven Audio Editing & Precision Alignment (Phase 5 - Planned for v4.0) [NOT STARTED]
   [ ] 7.1 "Text-as-Timeline" Rough-Cut Editing Engine
   [ ] 7.2 Seamless Non-Destructive Playback Preview
   [ ] 7.3 CTC Word Boundary Refinement & Forced Alignment
   [ ] 7.4 Memory-Bounded Diarization & Chunked Streaming

================================================================================
CORE ARCHITECTURAL INVARIANTS
================================================================================
- Core modules must NEVER import from plugins/.
- Heavy translation runtimes must remain isolated inside plugins/translation/.
- Speaker diarization uses wespeakerruntime as the primary ONNX embedding engine.
- Real-time progress updates with percentage, status messages, and estimated time.
- Diagnostic logs written to OS standard logging locations.
- Offline-first architecture: core processing functions locally without internet.
- 0 MB Default GPU Footprint: Base installer contains 0 MB of CUDA bloat; GPU features are purely optional and provisioned on demand.

================================================================================
1. PREVIOUS RELEASE: WINDOWS UPDATE SUPERVISOR & INSTALLER RETENTION FIX (Completed in v3.4.13)
================================================================================

[x] 1.1 HARDENED POWERSHELL SUPERVISOR EXIT LOOP
------------------------------------------------
- Upgraded PID wait supervisor in `launch_and_install` (`updater.py`) to an explicit `try { Get-Process -Id $pidToWait -ErrorAction Stop } catch { break }` exception loop, guaranteeing the supervisor blocks until `RadioTVSegmenter.exe` has completely exited before invoking UAC elevation.

[x] 1.2 POST-EXIT LOCK SETTLEMENT BUFFER
----------------------------------------
- Added a 2-second safety delay (`Start-Sleep -Seconds 2`) after application process exit to guarantee Windows kernel file handle cleanup and DLL unloads complete before triggering the update setup executable.

[x] 1.3 INNO SETUP PROCESS TERMINATION DIRECTIVE
------------------------------------------------
- Configured `CloseApplications=yes` and `CloseApplicationsFilter=*.exe` in `RadioTVStorySegmenter.iss` so setup runs cleanly signal and wait for active application instances.

[x] 1.4 MULTI-PACKAGE INSTALLER RETENTION
-----------------------------------------
- Relaxed auto-cleanup policy to `max_to_keep=2` across update checks and post-downloads, preserving recent downloaded installers in `AppData/Local/RadioTVSegmenter/updates` rather than purging previous packages.

================================================================================
2. CURRENT RELEASE: DESKTOP ROBUSTNESS, NON-BLOCKING IPC & POLISH (Completed in v3.4.14)
================================================================================

[x] 2.1 WINDOWS PATH ESCAPING FIX
---------------------------------
- Escaped single quotes in `Unblock-File` call inside `updater.py` (`escaped = str(path).replace("'", "''")`) to ensure reliable unblocking on paths with apostrophes.

[x] 2.2 NON-BLOCKING SINGLE-INSTANCE IPC & SOCKET RETENTION
-----------------------------------------------------------
- Migrated `waitForReadyRead(1000)` in `_handle_ipc_connection()` (`RadioTVSegmenter.py`) to asynchronous `readyRead` signal handling.
- Stored incoming client sockets in `window._pending_ipc_sockets` to prevent premature Python garbage collection.
- Connected `socket.disconnected` to `deleteLater` and removal from pending set.

[x] 2.3 TIMELINE SCROLL LOCK SAFEGUARD
--------------------------------------
- Wrapped `self.is_internal_scrollbar_update` modifications in `try...finally` blocks in `TimelineWidget.update_scrollbar_from_canvas` and `update_canvas_from_scrollbar` to prevent lock freezes on unexpected layout interruptions.

[x] 2.4 SUBPROCESS SET THREAD-SAFETY
------------------------------------
- Added a dedicated `threading.Lock()` (`_SUBPROCESS_LOCK`) guarding `_ACTIVE_SUBPROCESSES` mutations and iteration in `runtime_manager.py` (`register_process`, `unregister_process`, and `kill_all_subprocesses`), preventing race conditions and `RuntimeError: Set changed size during iteration` during multi-threaded teardowns.

[x] 2.5 PRUNE DEAD CODE & CLEAN DUPLICATE INITIALIZERS
------------------------------------------------------
- Removed unreferenced `setup_visual_fade_curve_combo()` in `prs_shared.py`.
- Eliminated redundant duplicate attribute assignments in `WaveformWorker.__init__`.

[x] 2.6 SYNC APPLICATION HEADER METADATA
----------------------------------------
- Refreshed legacy header docstrings in `RadioTVSegmenter.py` and `transcript_story.py` to match current project version.

[x] 2.7 GITHUB ACTIONS CI/CD HARDENING
--------------------------------------
- Replaced the fragile `FedericoCarboni/setup-ffmpeg@v3` action with a direct PowerShell static binary download from `Tyrrrz/FFmpegBin` (`7.0.2`), matching the proven pattern used on macOS and completely eliminating runner assertion errors (`Cannot get latest release` and `Requested version is not available`).
- Removed conflicting legacy Node 20 environment variables (`ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION` and `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`), standardizing runners on Node 24 without configuration warnings.

================================================================================
3. UPCOMING MILESTONE: AUTOMATED TEST RUNNER & PERFORMANCE BENCHMARK SUITE [PLANNED for v3.4.15]
================================================================================

[ ] 3.1 ZERO-DEPENDENCY CORE TEST RUNNER (`test_runner.py`)
----------------------------------------------------------
- Build a unified test runner using Python's standard `unittest` and `QTest` executing against sandboxed temporary environments (`tempfile.TemporaryDirectory()`).
- Implement an in-memory synthetic audio generation fixture using Python standard `wave` and `math.sin` (3-second 16kHz speech-like test audio generated in < 10ms with zero disk bloat).
- Implement Tier 1 Pure Logic Tests:
  * Project serialization / deserialization round-tripping for `.rtvs` files (JSON and gzip-compressed).
  * Fuzz testing for `format_time()` and `parse_time()` edge cases (`0.0`, `59.999`, `3599.999`, `86400.0`, negative values, malformed inputs).
  * Safe filename sanitization for Windows-reserved characters (`< > : " / \ | ? *`) and traversal paths (`..`).
  * Subtitle SRT and WebVTT timestamp formatting, sequence ordering, and text escaping.
  * WordPress REST API payload formatting (`data-nosnippet`, audio disclaimer placement, excerpt extraction).
- Implement Tier 2 Worker & Subprocess Concurrency Tests:
  * Bundled executable and system path resolution (`find_bundled_executable("ffmpeg")`, `ffprobe_path()`).
  * `WaveformWorker` synthetic audio peak computation and cancellation responsiveness (< 500ms FFmpeg shutdown).
  * `runtime_manager` process lifecycle and thread-safe lock stress tests (`register_process`, `unregister_process`, `kill_all_subprocesses`).
- Implement Tier 3 Headless Qt UI & Event Loop Tests (`QT_QPA_PLATFORM=offscreen`):
  * `InteractiveTranscriptEdit` focus isolation and scroll-lock position retention during simulated dialog triggers.
  * `TimelineWidget` scrollbar-to-canvas and canvas-to-scrollbar lock cycle verification (`try...finally` re-entrancy).
  * `QUndoStack` command push and undo/redo execution without unexpected playhead seek jumps.
- Implement Tier 4 Native ML Runtime Smoke Tests:
  * Harness integration with `RadioTVSegmenter.py --self-test` (PyTorch, CTranslate2, Silero VAD, WeSpeaker ONNX runtime).
- Provide clean CLI flags:
  * `python test_runner.py --quick` (Tier 1 pure logic, sub-second execution).
  * `python test_runner.py --all` (Tiers 1–3, comprehensive local test).
  * `python test_runner.py --self-test` (Tier 4 native ML runtime verification).

[ ] 3.2 PERFORMANCE & SPEED BENCHMARK ENGINE (`benchmark.py` / CLI & Diagnostic GUI)
-------------------------------------------------------------------------------------
- Implement a lightweight, high-precision performance benchmarking harness using standard `time.perf_counter()` and `tracemalloc`.
- Real-Time Factor (RTF) Calculation Engine:
  * Calculate standard audio industry RTF: $\text{RTF} = \text{Audio Duration} / \text{Processing Time}$.
- Core Throughput Benchmarks:
  * Waveform Extraction Throughput: Stream synthetic audio through FFmpeg peak extraction to measure MB/s and RTF.
  * VAD Chunk Processing Latency: Measure Silero Voice Activity Detection chunk evaluation time per audio second.
  * Speaker Diarization Latency: Measure WeSpeaker ONNX embedding extraction speed (embeddings/second).
  * Project I/O Throughput: Measure serialization and deserialization speeds across large segment counts (segments/second).
  * UI Rendering & Document Reflow Latency: Measure text formatting and layout recalculation time in `InteractiveTranscriptEdit` (words/second).
- Formatted Diagnostic Output:
  * Terminal dashboard displaying subsystem timings, memory deltas, RTF values, and system specifications (CPU model, thread count, OS, GPU/CUDA availability).
  * Optional JSON export (`--json`) for automated regression tracking across release builds.
- (Optional GUI Dialog):
  * Add Help -> "Run System Performance Benchmark..." action launching a non-blocking background benchmark dialog with progress bar and copyable summary report.

[ ] 3.3 CONTINUOUS MAINTENANCE & FUTURE FEATURE TEST COVERAGE POLICY
--------------------------------------------------------------------
- **Mandatory Synchronous Test Addition**: Whenever a future release introduces a new processing algorithm, timeline feature, export plugin, or UI interaction component, corresponding test cases MUST be synchronously added to `test_runner.py` and benchmark checkpoints added to `benchmark.py`.
- **Regression Guard**: All CI and pre-release verification workflows must pass `test_runner.py --quick` and `test_runner.py --all` prior to creating stable release snapshots or tagging versions.

================================================================================
4. FUTURE MILESTONE: PHASE 2 MODULARIZATION OF PRS_SHARED.PY (UI & Qt Components) [PLANNED]
================================================================================

Proceed with extreme caution: unlike Phase 1's pure-Python modules, Phase 2 components are tightly coupled to PySide6/Qt signals, slots, widget parenting hierarchies, and paint events.

[ ] 4.1 CAUTIOUS TESTING & VERIFICATION METHODOLOGY (Protocol for Every Single Extraction)
------------------------------------------------------------------------------------------
To avoid cascading regressions or subtle Qt widget crashes, execute the following 5-step cycle for EACH extracted component module:
1. **Isolated Module Extraction**:
   - Extract only ONE logical domain at a time (e.g. `cache_manager` first, which has minimal Qt widget hierarchy).
   - Identify all internal dependencies and verify whether they need to import from `prs_shared.py` or base Qt modules.
2. **Re-Export Shim Installation**:
   - Add explicit re-export shims in `prs_shared.py` (`from <new_module> import *`) immediately so external callers remain 100% intact.
3. **Multi-Tier Syntax & Compilation Verification**:
   - Run `python -m py_compile <new_module>.py prs_shared.py RadioTVSegmenter.py ui_layout.py transcript_story.py processing.py`.
4. **Headless Import & Symbol Verification**:
   - Run automated headless script checking:
     * Module importability without cyclical import errors.
     * All expected classes, signals, and functions are present and callable.
     * Instantiation of non-widget classes and default dataclasses/namedtuples.
5. **Interactive UI Smoke Check & Snapshot**:
   - Verify widget instantiation in offscreen/test mode.
   - Run web preview linter & compiler (`lint_applet` & `compile_applet`).
   - Create a rollback checkpoint or snapshot before moving to the next module.

[ ] 4.2 TARGET MODULE EXTRACTION SEQUENCE
-----------------------------------------
- **Step 1: `cache_manager.py` (Low-to-Medium Risk)**:
  * Targets: `format_byte_size`, `get_cache_disk_usage`, `cleanup_old_thumbnail_cache`, `ClearCacheDialog`, `VideoThumbnailCache`.
  * Dependencies: PySide6 QDialog/QThread, `core_utils`.
- **Step 2: `story_widgets.py` (Medium Risk)**:
  * Targets: `StoryListWidget`, `StoryItemWidget`, `StoryCardWidget`, `StoryMetadataDialog`, drag-and-drop file queues.
  * Dependencies: PySide6 QListWidget, custom signals (`filesDropped`), styling sheets.
- **Step 3: `timeline_widgets.py` (Medium-to-High Risk)**:
  * Targets: `TimelineRulerWidget`, `TimelineTrackWidget`, `PlayheadOverlay`, waveform peak painting, coordinate-to-time mapping.
  * Dependencies: QPainter, custom paint events, audio time conversion.
- **Step 4: `transcript_editor.py` (High Risk)**:
  * Targets: `TranscriptTextViewer`, `TranscriptSegmentWidget`, word-level span highlighting, search-and-replace dialog.
  * Dependencies: QTextEdit/QPlainTextEdit, cursor manipulation, speaker coloring, audio synchronization slots.

================================================================================
5. FUTURE MILESTONE: GOOGLE DOCS & DRIVE EXPORT PLUGIN (Planned for v3.5) [NOT STARTED]
================================================================================

Architectural Objective:
Implement Google Docs & Drive export as a dedicated, self-contained export plugin (`plugins/gdocs/`) rather than integrating directly into the core desktop binary.

Key Architectural & Packaging Benefits:
- Core Binary Savings: Avoids bundling official Google client libraries (`google-api-python-client`, `google-auth`, etc.) or cloud SDK stubs in the core PyInstaller distribution, saving 20-45 MB uncompressed disk footprint and 10-18 MB in the installer executable.
- Zero Memory / Startup Impact: Utilizes RTVS lazy plugin registration so offline-first users incur zero overhead or dependency initialization at launch.
- Security & Credential Isolation: Encapsulates OAuth 2.0 client credentials, user authorization scopes (`drive.file`, `documents`), and refresh tokens strictly within plugin-scoped secure OS keyring storage.
- Independent Release Cadence: Allows updating OAuth flows or Google Docs API formatting logic via standalone `.rtvs-addon` packages without requiring a rebuild of the full native desktop app.

[ ] 5.1 Plugin Scaffolding & OAuth 2.0 Auth Engine (`plugins/gdocs/auth.py`)
-------------------------------------------------------------------------
- Create `plugins/gdocs/manifest.json` (category: "export", lazy-enabled) and plugin base class implementation in `plugins/gdocs/plugin.py`.
- Build an isolated OAuth 2.0 authorization module supporting loopback redirect on localhost (`127.0.0.1:port`) for seamless browser-based user sign-in.
- Securely persist refresh tokens using the OS keyring (`keyring` module already present in core requirements) with manual disconnect/revoke capability in plugin preferences.

[ ] 5.2 Google Docs Document Serializer & REST Exporter (`plugins/gdocs/formatter.py`, `client.py`)
-------------------------------------------------------------------------------------------------
- Implement lightweight Google Docs REST client using Python's standard `requests` library (avoiding heavy external Google SDK dependencies).
- Serialize segmented transcript data into structured Google Docs `batchUpdate` requests:
  * Document header: Broadcast title, date, total duration, detected speakers, and story summary table.
  * Story headings & cards: Heading styles, story metadata tables (word count, duration, in/out points).
  * Styled transcript body: Speaker name badges with distinct colors, italicized timecode markers, and preserved word-level text highlights (`backgroundColor` style updates matching Word/DOCX styles).

[ ] 5.3 Native Margin Comments & Annotation Anchoring (`plugins/gdocs/comments.py`)
---------------------------------------------------------------------------------
- Map RTVS story notes and transcript inline comments directly to native Google Drive margin comments via `drive.comments.create`.
- Anchor comments to corresponding text ranges or paragraph spans in the generated document using Google Docs document range offsets.

[ ] 5.4 Unified Export Center & Menu Integration
----------------------------------------------
- Register the Google Docs export destination in the Unified Export Center and the main window Transcript menu ("Export" -> "Export to Google Docs...").
- Provide a non-blocking asynchronous export worker with progress bar updates, rate-limiting retry handling, and direct "Open in Google Docs" link generation upon completion.

[ ] 5.5 (Optional) One-Time Review & Comment Pull ("Import Reviews from Google Doc")
-----------------------------------------------------------------------------------
- Add an optional menu action to fetch updated margin comments or suggested edits from a linked Google Doc.
- Present changes in a non-destructive review diff dialog, allowing the editor to selectively accept or dismiss comments without breaking word-level audio alignment.

================================================================================
6. FUTURE MILESTONE: EXPANDED NLE & DAW TIMELINE INTERCHANGE (Phase 4 - Planned for v3.6) [NOT STARTED]
================================================================================

- Generates non-destructive timeline project files to import translated or diarized stories seamlessly into professional post-production workflows.
- 6.1 Cockos Reaper (.rpp) Generator
- 6.2 Magix Samplitude EDL (.edl)
- 6.3 Video NLE Interchange (FCPXML / Premiere Pro)
- 6.4 Avid / Pro Tools AAF Composition

================================================================================
7. FUTURE MILESTONE: TEXT-DRIVEN AUDIO EDITING & PRECISION ALIGNMENT (Phase 5 - Planned for v4.0) [NOT STARTED]
================================================================================

- "Text-as-Timeline" Rough-Cut Editing Engine
- Seamless Non-Destructive Playback Preview
- CTC Word Boundary Refinement & Forced Alignment
- Memory-Bounded Diarization & Chunked Streaming

