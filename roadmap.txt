================================================================================
RADIO & TV STORY SEGMENTER - DEVELOPMENT ROADMAP & TECHNICAL SPECIFICATION
================================================================================
This document outlines the architectural roadmap and implementation specifications
for future versions of the Radio & TV Story Segmenter. Adapted from best-in-class
heuristics, modular runtime architectures, and timeline serialization algorithms.

--------------------------------------------------------------------------------
ROADMAP MAINTENANCE POLICY: N-1 SLIDING-WINDOW & STATUS TRACKING
--------------------------------------------------------------------------------
- The roadmap maintains a rolling window: it displays the current/immediate past
  release ($N-1$) for baseline context, followed by active upcoming development
  milestones ($N$, $N+1$).
- Status Notation Policy (Modeled after Social Digest):
  * All milestones and feature items are annotated with explicit progress indicators:
    - [x] Completed item / feature
    - [-] In-progress item / active development
    - [ ] Planned / not started item
  * When work commences on an item, mark it immediately as [-] [IN-PROGRESS].
  * Mark an item as [x] [COMPLETED] when implementation is finalized, upon user
    confirmation, or inferred when development transitions to another part of the roadmap.
- When a new version is built, its completed specifications are recorded, and any
  versions older than $N-1$ are pruned to keep the document focused and actionable.
- Whenever any code change, bug fix, packaging adjustment, or feature refinement is
  made during a release cycle, it MUST be recorded immediately in the current release
  section of roadmap.txt and CHANGELOG.md (even if it occurs after the initial version
  number bump).

--------------------------------------------------------------------------------
TABLE OF CONTENTS & STATUS OVERVIEW
--------------------------------------------------------------------------------
1. Previous Release: Rapid Dialog Turn Context Redesign, Speaker Manager Ergonomics & Voice Matcher Performance (`transcript_story.py`) [x] [COMPLETED in v3.7.10-beta]
   [x] 1.1 Refine Rapid Dialog Turn Context & Timestamp Redesign: Replaced raw numeric segment index spinboxes with rich dropdown selectors showing timestamps, speaker labels, and spoken word snippets for both Start and End turns, alongside an interactive live preview card displaying turn counts, time spans, durations, and boundary quote snippets.
   [x] 1.2 Speakers & Detection Clusters Table Ergonomics & Legibility: Normalized table row heights (`setRowHeight(row, 46)`), set vertical header default section size to 46px, allocated a fixed 240px width to column 4, and restyled action buttons with crisp contrast, rounded borders, and pointing hand cursors to eliminate button clipping.
   [x] 1.3 Acoustic Voice Profile Matcher Performance & Fluidity: Implemented 60ms debounced slider scrubbing for 60+ FPS responsiveness, precomputed base competitor speaker centroids in `find_matching_voice_turns` to eliminate redundant vector math, added flicker-free batch table updates, guarded against spurious audio player seeks during updates, and retained explicit user unchecks across sensitivity adjustments.

2. Current Release: Unified Voice Profile Tools Submenu & Multi-Entrypoint Integration (`transcript_editor.py`, `ui_layout.py`, `transcript_story.py`, `processing.py`) [x] [COMPLETED in v3.7.11-beta]
   [x] 2.1 Unified Editor Context Submenu: Grouped all acoustic voice profiling, reference profile creation, rapid dialog turn refinement, and cluster inspection actions into a single organized `🎙️ Voice Profile Tools` submenu in the transcript editor context menu.
   [x] 2.2 Standardized Action Descriptors & Tooltips: Unified labels for `Teach / Match This Voice Across Project...`, `Set as Reference Profile for New Speaker...`, `Refine Rapid Dialog Turns (A/B Classifier)...`, and `Manage Speakers & Detection Clusters...`.
   [x] 2.3 Top Menu Bar Integration (`Tools -> Voice Profile Tools`): Added the dedicated `🎙️ Voice Profile Tools` submenu to the main application Tools menu in `ui_layout.py` with keyboard accessibility.
   [x] 2.4 Context-Aware Active Turn Inference: Added robust fallback in `teach_voice_profile_dialog` to infer active turn from the transcript text cursor or playback head when invoked directly from the top menu bar.
   [x] 2.5 Pipeline State & Action Synchronization: Integrated `voice_tools_menu` and child actions into `set_tools_actions_enabled` in `processing.py`.

3. Upcoming Milestone: Comprehensive Cloud & Local Backup & Restore System (`project_lifecycle.py`, `plugins/gdocs/`) [PLANNED for v3.8.0] [NOT STARTED]
   [ ] 3.1 Unified State Serialization (`project_lifecycle.py`): Export preferences, keyboard shortcuts, installed model inventories, glossary items, and user configurations into a cryptographically verified backup archive.
   [ ] 3.2 Manual Local Backup & Restore: Download backup archives locally and restore previous settings and system state into RTVS.
   [ ] 3.3 1-Click Google Drive Cloud Restore: Connect via Google Drive API to browse, verify via SHA-256 hash matching, and restore custom settings directly from cloud backups.

4. Future Milestone: Multi-Platform Timeline & DAW Interchange Engine (`export/timeline.py`, `export/dialog.py`, `ui_layout.py`) [PLANNED for v3.8.5] [NOT STARTED]
   [ ] 4.1 Migration to Unified Export Center Tab: Move timeline and DAW exports from the legacy File menu submenu into a dedicated "Timeline & DAW" tab in the Unified Export Center dialog, streamlining menu navigation and providing a unified UI for multi-format timeline exporting.
   [ ] 4.2 Streamlined Multi-Platform Timeline Format Exporters (Zero Feature Loss):
       - Final Cut Pro 7 XML Interchange (.xml / xmeml): Adobe Premiere Pro, Adobe Audition, DaVinci Resolve, Descript (Full multi-track sequences, cuts, clip positions, volume envelopes, crossfades, and marker tracks).
       - Final Cut Pro X XML (.fcpxml): Apple Final Cut Pro (FCPX), Apple Logic Pro (Native modern Apple timeline sequence with multi-channel audio components, retimed clips, markers, and volume automation without third-party converters).
       - Universal AAF Interchange (.aaf) & Session Text: Avid Pro Tools, Apple Logic Pro, Avid Media Composer, Cubase/Nuendo (Full broadcast timeline session interchange, multi-track stems, non-destructive clip gain, fades, and track markers via `pyaaf2`).
       - Hindenburg Broadcast Session (.nhx): Hindenburg Journalist, Hindenburg PRO (Voice-optimized radio/podcast tracks, non-destructive speech edits, voice level envelopes, and chapter markers).
       - Cockos REAPER Project (.rpp): Cockos REAPER (Complete non-destructive project files with item fades, volume envelopes, markers, regions, and take notes).
       - Magix Samplitude / Sequoia EDL (.edl v1.5): Magix Samplitude, Sequoia (Broadcast audio edit decision list with crossfades and track gain).
       - Audacity Labels & Podcast Cues (.txt / .cue): Audacity, Podcasting mastering tools, CD/playout automation (Standard label tracks and CUE sheets with chapter markers and boundary synchronization).
   [ ] 4.3 Future-Proofed Non-Destructive Editing & Timeline Engine:
       - Multi-track routing layouts (Single track, Checkerboard A/B roll, Split tracks per identified speaker).
       - Unselected audio & rough cut handling (Omit/Ripple-delete, Include as Muted Items for sync preservation, or Route to separate B-Roll reference track).
       - Clip gain, volume automation curves, and non-destructive fade handles (linear, logarithmic, exponential, S-curve, equal-power).
       - Embedded story metadata & notes (Inject synopsis, speaker labels, tags, and transcript excerpts into DAW item notes/takes).
       - Source media linking flexibility (Absolute paths, Relative paths for portable session packages, or Auto-rendered isolated WAV stems).
       - Rational timecode & audio sample rate math (44.1, 48, 96 kHz; 23.976, 24, 25 PAL, 29.97 NDF/DF, 30, 59.94 fps, custom start timecode e.g. 01:00:00:00).

5. Future Milestone: Text-Driven Audio Editing & Precision Alignment (Phase 5) [PLANNED for v4.0.0] [NOT STARTED]
   [ ] 5.1 "Text-as-Timeline" Rough-Cut Editing Engine
   [ ] 5.2 Seamless Non-Destructive Playback Preview
   [ ] 5.3 CTC Word Boundary Refinement & Forced Alignment
   [ ] 5.4 Memory-Bounded Diarization & Chunked Streaming

================================================================================
DETAILED MILESTONE SPECIFICATIONS
================================================================================

================================================================================
1. PREVIOUS RELEASE: TEST BENCH FROZEN-PATH & ACOUSTIC MOCK SIGNATURE FIXES [x] [COMPLETED in v3.7.9-beta]
================================================================================

[x] 1.1 Resilient Subtitle & DAW Module Resolution (`test_runner.py`)
--------------------------------------------------------------------------------
- Implemented `_resolve_export_subtitles_module()` and `_resolve_export_daw_module()` in `test_runner.py` to attempt native python package imports before falling back to filesystem path resolutions (`_MEIPASS`, `_internal`, relative paths).
- Resolved `FileNotFoundError: [Errno 2] No such file or directory: 'C:\Program Files\Radio & TV Segmenter\export/subtitles.py'` when running test bench inside installed production folders.

[x] 1.2 MockTranscriptWindow Activity Logging Signature Harmonization (`test_runner.py`)
--------------------------------------------------------------------------------
- Harmonized `MockTranscriptWindow.log_activity(self, msg, *args, **kwargs)` in `_test_acoustic_voice_profile_matcher` to gracefully accept keyword arguments such as `mark_dirty=False`.
- Resolved `TypeError` during live Qt mixin testing with `TranscriptStoryMixin.register_confirmed_speaker_turn()`.

[x] 1.3 Full Diagnostic Suite Verification (39 Tests)
--------------------------------------------------------------------------------
- Verified 39 tests with 100% pass/warning cleanliness across isolated execution directories.

================================================================================
2. CURRENT RELEASE: RAPID DIALOG TURN CONTEXT REDESIGN, SPEAKER MANAGER ERGONOMICS & VOICE MATCHER PERFORMANCE [x] [COMPLETED in v3.7.10-beta]
================================================================================

[x] 2.1 Refine Rapid Dialog Turn Context & Timestamp Redesign (`prompt_refine_speaker_run` in `transcript_story.py`)
--------------------------------------------------------------------------------
- Replaced raw numeric segment index spinboxes with rich dropdown selectors displaying formatted timestamps, speaker labels, and spoken text snippets for both Start and End turns (e.g. `[00:14.200] Turn #4 (Leticia): “Hello, how are you today…”`).
- Added an interactive live range & context preview card displaying turn counts, time spans, durations, and boundary quote snippets that updates synchronously when dropdown items change.
- Automated bounds normalization (`min(s_i, e_i), max(s_i, e_i)`) ensuring seamless execution regardless of selection order.

[x] 2.2 Speakers & Detection Clusters Table Ergonomics & Legibility (`SpeakerManagerDialog` in `transcript_story.py`)
--------------------------------------------------------------------------------
- Normalized table row heights (`setRowHeight(row, 46)`), set vertical header default section size to 46px, and allocated a fixed 240px width to column 4.
- Restyled "Rename / Alias" and "Merge Into..." buttons with crisp typography, high-contrast dark slate backgrounds, rounded borders, 26px minimum height, and pointing hand cursors to eliminate vertical button clipping and ensure effortless legibility.

[x] 2.3 Acoustic Voice Profile Matcher Performance & Fluidity (`VoiceProfileMatchDialog` & `find_matching_voice_turns` in `transcript_story.py`)
--------------------------------------------------------------------------------
- Implemented 60ms single-shot debouncing for similarity threshold slider scrubbing, decoupling immediate label updates from heavy table updates for fluid 60+ FPS responsiveness.
- Precomputed base competitor centroids and top-turn sets in `find_matching_voice_turns` outside the candidate loop, eliminating redundant 256-dimensional vector calculations.
- Optimized table population with `setUpdatesEnabled(False)` and pre-allocated row counts (`setRowCount(len(self.matched_turns))`) to eliminate layout thrashing.
- Guarded audio player seeking during table updates to suppress spurious player seeks.
- Preserved user turn deselections across sensitivity adjustments via `_user_deselected_ids`.

================================================================================
3. UPCOMING MILESTONE: COMPREHENSIVE CLOUD & LOCAL BACKUP & RESTORE SYSTEM [PLANNED for v3.8.0]
================================================================================

[ ] 3.1 Unified State Serialization & Archive Packaging (`project_lifecycle.py`)
---------------------------------------------------------------------------------
- Serialize user preferences, keyboard shortcuts, installed model inventory, custom glossary items, and user configuration states into a single cryptographically verified backup archive.

[ ] 3.2 Manual Local Backup & Restore Utility (`ui_layout.py`, `project_lifecycle.py`)
-------------------------------------------------------------------------------------
- Provide straightforward File menu actions to export complete system backup bundles locally and import/restore previous settings and system states into RTVS.

[ ] 3.3 1-Click Google Drive Cloud Restore & SHA-256 Verification (`plugins/gdocs/`)
-----------------------------------------------------------------------------------
- Integrate with Google Drive API to browse, download, and restore backups directly from cloud storage using custom filenames and SHA-256 integrity hash validation.
