================================================================================
RADIO & TV STORY SEGMENTER - DEVELOPMENT ROADMAP & TECHNICAL SPECIFICATION
================================================================================
This document outlines the architectural roadmap and implementation specifications
for future versions of the Radio & TV Story Segmenter. Adapted from best-in-class
heuristics, modular runtime architectures, and timeline serialization algorithms.

--------------------------------------------------------------------------------
ROADMAP MAINTENANCE POLICY: N-1 SLIDING-WINDOW & STATUS TRACKING
--------------------------------------------------------------------------------
- The roadmap maintains a rolling window: it displays the current/immediate past
  release ($N-1$) for baseline context, followed by active upcoming development
  milestones ($N$, $N+1$).
- Status Notation Policy (Modeled after Social Digest):
  * All milestones and feature items are annotated with explicit progress indicators:
    - [x] Completed item / feature
    - [-] In-progress item / active development
    - [ ] Planned / not started item
  * When work commences on an item, mark it immediately as [-] [IN-PROGRESS].
  * Mark an item as [x] [COMPLETED] when implementation is finalized, upon user
    confirmation, or inferred when development transitions to another part of the roadmap.
- When a new version is built, its completed specifications are recorded, and any
  versions older than $N-1$ are pruned to keep the document focused and actionable.
- Whenever any code change, bug fix, packaging adjustment, or feature refinement is
  made during a release cycle, it MUST be recorded immediately in the current release
  section of roadmap.txt and CHANGELOG.md (even if it occurs after the initial version
  number bump).

--------------------------------------------------------------------------------
TABLE OF CONTENTS & STATUS OVERVIEW
--------------------------------------------------------------------------------
1. Previous Release: Story Audio Fade-In/Out & Per-Story Envelope Controls (Completed in v3.3.0)
   [x] 1.1 Global & Export-Time Fade Controls with Performance Notice (Toggle in Unified Export Dialog with UI notice: "Re-encoding required; export may be slower")
   [x] 1.2 Configurable Default Fade Durations in Preferences (Settings -> Detection & Diarization: Default Story Fade-In & Fade-Out duration spinboxes)
   [x] 1.3 Interactive Timeline Fade Handles for In & Out (Dual tactile handles along top edge of story blocks with live waveform ramp overlays and drag sizing)
   [x] 1.4 Per-Story Fade Durations in Stories Menu & Context Menu (Right-click menu / story dialog for sample-accurate numeric entry of both fade-in and fade-out with Undo/Redo)
   [x] 1.5 Chained FFmpeg Audio Filter Graph (`afade=t=in...` and `afade=t=out...` with `-c:v copy` and container-native audio codecs)

2. Current Release: Real-Time Audio Fade Playback Preview & Auditioning System (Completed in v3.3.1)
   [x] 2.1 Dynamic Gain Modulation During Playback (`get_fade_volume_factor_at_time(t)` and 25ms `fade_preview_timer` tracking fade envelopes in `QAudioOutput`)
   [x] 2.2 Story Clip Auditioning (`audition_story` via story list right-click context menu with automatic boundary stop and fade ramp auditioning)
   [x] 2.3 Master Volume Preservation & Seamless Reset (Restores `master_volume` on stop/pause or exiting fade regions)
   [x] 2.4 Playback & Timeline Preferences for Audio Fades (Toggles for `enable_audio_fades` and `preview_audio_fades` in Preferences, persisted in `QSettings`)
   [x] 2.5 Timeline Canvas Performance & Tooltip Debouncing (Viewport bounding box culling on envelope hit-testing and painting; debounced mouseMove tooltips)

3. Future Milestone: Expanded NLE & DAW Timeline Interchange (Phase 4 - Planned for v3.4) [NOT STARTED]
   [ ] 3.1 Cockos Reaper Project (.rpp) Generator
   [ ] 3.2 Magix Samplitude EDL (v1.5) Export
   [ ] 3.3 Video NLE Interchange (FCPXML, Premiere / DaVinci Resolve xmeml XML)
   [ ] 3.4 Avid / Pro Tools AAF Composition
   [ ] 3.5 Rational Timecode & Audio Sample Rate Math
   [ ] 3.6 UI & Batch Export Integration

4. Future Milestone: Text-Driven Audio Editing & Precision Alignment (Phase 5 - Planned for v4.0) [NOT STARTED]
   [ ] 4.1 "Text-as-Timeline" Rough-Cut Editing Engine
   [ ] 4.2 Seamless Non-Destructive Playback Preview
   [ ] 4.3 CTC Word Boundary Refinement & Forced Alignment
   [ ] 4.4 Memory-Bounded Diarization & Chunked Streaming

================================================================================
CORE ARCHITECTURAL INVARIANTS
================================================================================
- Core modules must NEVER import from plugins/.
- Heavy translation runtimes must remain isolated inside plugins/translation/.
- Speaker diarization uses wespeakerruntime as the primary ONNX embedding engine.
- Real-time progress updates with percentage, status messages, and estimated time.
- Diagnostic logs written to OS standard logging locations.
- Offline-first architecture: core processing functions locally without internet.
- 0 MB Default GPU Footprint: Base installer contains 0 MB of CUDA bloat; GPU features are purely optional and provisioned on demand.

================================================================================
1. PREVIOUS RELEASE: STORY AUDIO FADE-IN/OUT & PER-STORY ENVELOPE CONTROLS (Completed in v3.3.0)
================================================================================

[x] 1.1 GLOBAL & EXPORT-TIME FADE CONTROLS WITH PERFORMANCE NOTICE
-------------------------------------------------------------------
- Added an explicit toggle in the Unified Export Dialog (`export/dialog.py`) next to media format options: `[x] Apply audio fade-in & fade-out`.
- Included prominent helper tooltip in the UI: *"Renders smooth audio fade ramps at start and end of exported story media clips."*
- Remembers user selection across application sessions using `QSettings`.

[x] 1.2 CONFIGURABLE DEFAULT FADE DURATIONS IN PREFERENCES
------------------------------------------------------------
- Exposed configurable default duration spinboxes in `playback_preferences.py` under the Detection & Diarization settings tab:
  * Default Story Fade-In Duration: `0.0s` (Range: `0.0s` - `10.0s`)
  * Default Story Fade-Out Duration: `1.0s` (Range: `0.0s` - `10.0s`)
- Values persisted to user preferences via `QSettings` and automatically applied as default properties when creating or detecting stories.

[x] 1.3 INTERACTIVE TIMELINE FADE HANDLES (DUAL CORNER MANIPULATION)
----------------------------------------------------------------------
- Added dual tactile drag-and-drop fade handles to each story block in the waveform/timeline canvas:
  * Top-Left Apex: Fade-In handle (drag horizontally to lengthen/shorten fade-in ramp).
  * Top-Right Apex: Fade-Out handle (drag horizontally to lengthen/shorten fade-out ramp).
- Hovering within 8px of either handle activates a horizontal sizing cursor (`Qt.SizeHorCursor`).
- Real-time rendering of diagonal dashed fade ramp lines and darkened attenuation masks directly on the waveform showing exact envelope shapes.
- Supported with full `StoryFadesChangeCommand` undo/redo stack recording.

[x] 1.4 PER-STORY FADE DURATIONS VIA STORIES MENU & CONTEXT MENU
-----------------------------------------------------------------
- Provided sample-accurate numerical entry dialog via right-click context menu on timeline story segments and story list items (`Set Audio Fades...`).
- Implemented `StoryFadesDialog` with precision decimal spinboxes for Fade-In (s) and Fade-Out (s) with quick presets (`No Fades (0s)`, `Restore Defaults`), duration clamping, and optional project-wide application.

[x] 1.5 CHAINED FFMPEG AUDIO FILTER PIPELINE INTEGRATION
----------------------------------------------------------
- Updated `extract_media()` in `project_export.py` to evaluate both `fade_in` and `fade_out` durations.
- Dynamically constructs chained audio filter arguments: `afade=t=in:ss=0:d={in_dur:.3f}` and `afade=t=out:st={duration - out_dur:.3f}:d={out_dur:.3f}`.
- Keeps video tracks stream-copied (`-c:v copy`) for video containers (`.mp4`, `.mov`, etc.), re-encoding only the audio stream to native container formats (`aac`, `libmp3lame`, `pcm_s16le`, `libopus`).

================================================================================
2. CURRENT RELEASE: REAL-TIME AUDIO FADE PLAYBACK PREVIEW & AUDITIONING SYSTEM (Completed in v3.3.1)
================================================================================

[x] 2.1 DYNAMIC GAIN MODULATION DURING PLAYBACK
-------------------------------------------------
- Implemented real-time volume gain calculation (`get_fade_volume_factor_at_time(t)`) evaluating story fade-in and fade-out envelope curves.
- Connected a 25ms timer (`fade_preview_timer`) during playback to smoothly modulate `QAudioOutput.setVolume(...)` as the playhead traverses fade ramps.

[x] 2.2 STORY CLIP AUDITIONING WITH FADE RAMPS
------------------------------------------------
- Added *"Audition Story (With Fades ▶)"* to the story list right-click context menu.
- Auditions target story from start to finish with fade envelope curves active and stops playback automatically at story boundary (`end + 0.05s`).

[x] 2.3 MASTER VOLUME PRESERVATION & SEAMLESS RESET
----------------------------------------------------
- Caches user-configured master playback volume (`master_volume`).
- Automatically restores volume upon pausing, stopping playback, or seeking outside fade regions.

[x] 2.4 PLAYBACK & TIMELINE PREFERENCES FOR AUDIO FADES
--------------------------------------------------------
- Added toggles in Preferences -> *"Playback & Timeline"*:
  * `[x] Enable story audio fades and timeline envelope handles` (`enable_audio_fades`)
  * `[x] Preview audio fades in real-time during playback` (`preview_audio_fades`)
- Dynamically toggles timeline envelope grab handles, dark gain masks, diagonal dashed ramp overlays, story delegate fade indicators, and export processing.

[x] 2.5 TIMELINE CANVAS PERFORMANCE & TOOLTIP DEBOUNCING
---------------------------------------------------------
- Viewport bounding box culling on envelope hit-testing (`find_fade_handle_at_pos`) and painting (`end_x < -16 or start_x > width + 16`).
- Debounced hover tooltips on the timeline canvas to avoid continuous OS redraw events during scrubbing and playback.

================================================================================
3. FUTURE MILESTONE: EXPANDED NLE & DAW TIMELINE INTERCHANGE (Phase 4 - Planned for v3.4) [NOT STARTED]
================================================================================

[ ] 3.1 COCKOS REAPER PROJECT (.rpp) GENERATOR
-----------------------------------------------
Upgrades the simple EDL marker export to full multi-track Reaper project files.
- RPP Structure: Root `<REAPER_PROJECT 0.1 "7.0">`, `<TRACK`, `<ITEM` definitions with micro-fades.

[ ] 3.2 MAGIX SAMPLITUDE EDL (v1.5) EXPORT
-------------------------------------------
Sample-accurate text-based cut-list widely supported by legacy radio broadcast consoles and DAW tools.

[ ] 3.3 VIDEO NLE INTERCHANGE (FCPXML & XMEML XML)
---------------------------------------------------
Generates sequence timelines for video newsrooms and multi-cam podcast edits (FCPXML v1.9/1.10 and Adobe Premiere/Resolve XML).

[ ] 3.4 AVID & PRO TOOLS AAF COMPOSITION
-----------------------------------------
Support AAF composition export for high-end post-production facilities.

[ ] 3.5 RATIONAL TIMECODE & AUDIO SAMPLE RATE MATH
---------------------------------------------------
Rational time base conversions to ensure sub-millisecond sync across DAW and NLE workflows.

[ ] 3.6 UI & BATCH EXPORT INTEGRATION
--------------------------------------
Expand Export Menu in `RadioTVSegmenter.py` with direct DAW and NLE options.

================================================================================
4. FUTURE MILESTONE: TEXT-DRIVEN AUDIO EDITING & PRECISION ALIGNMENT (Phase 5 - Planned for v4.0) [NOT STARTED]
================================================================================

[ ] 4.1 "TEXT-AS-TIMELINE" ROUGH-CUT EDITING ENGINE
----------------------------------------------------
Allows journalists to edit audio/video directly by striking through or deleting words in the transcript UI.

[ ] 4.2 SEAMLESS NON-DESTRUCTIVE PLAYBACK PREVIEW
--------------------------------------------------
Custom audio playback handler in PySide6 / QMediaPlayer playing strictly inside keep ranges with anti-click crossfades.

[ ] 4.3 CTC WORD BOUNDARY REFINEMENT & FORCED ALIGNMENT
--------------------------------------------------------
Lightweight acoustic phone alignment pass for sub-10ms boundary precision.

[ ] 4.4 MEMORY-BOUNDED DIARIZATION & CHUNKED STREAMING
-------------------------------------------------------
Chunked streaming diarization to avoid out-of-memory errors on long broadcast recordings.
