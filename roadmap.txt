================================================================================
RADIO & TV STORY SEGMENTER - DEVELOPMENT ROADMAP & TECHNICAL SPECIFICATION
================================================================================
This document outlines the architectural roadmap and implementation specifications
for future versions of the Radio & TV Story Segmenter. Adapted from best-in-class
heuristics, modular runtime architectures, and timeline serialization algorithms.

--------------------------------------------------------------------------------
ROADMAP MAINTENANCE POLICY: N-1 SLIDING-WINDOW & STATUS TRACKING
--------------------------------------------------------------------------------
- The roadmap maintains a rolling window: it displays the current/immediate past
  release ($N-1$) for baseline context, followed by active upcoming development
  milestones ($N$, $N+1$).
- Status Notation Policy (Modeled after Social Digest):
  * All milestones and feature items are annotated with explicit progress indicators:
    - [x] Completed item / feature
    - [-] In-progress item / active development
    - [ ] Planned / not started item
  * When work commences on an item, mark it immediately as [-] [IN-PROGRESS].
  * Mark an item as [x] [COMPLETED] when implementation is finalized, upon user
    confirmation, or inferred when development transitions to another part of the roadmap.
- When a new version is built, its completed specifications are recorded, and any
  versions older than $N-1$ are pruned to keep the document focused and actionable.
- Whenever any code change, bug fix, packaging adjustment, or feature refinement is
  made during a release cycle, it MUST be recorded immediately in the current release
  section of roadmap.txt and CHANGELOG.md (even if it occurs after the initial version
  number bump).

--------------------------------------------------------------------------------
TABLE OF CONTENTS & STATUS OVERVIEW
--------------------------------------------------------------------------------
1. Previous Release: Multi-Format DAW Interchange & Unselected Audio Modes (Completed in v3.5.5)
   [x] 1.1 Expanded DAW & NLE Interchange Formats (`export/daw.py`): Added native support for Audacity Label Tracks (`.txt`), Adobe Audition / Final Cut Pro / Premiere XML (`.xml`), and Universal DAW Marker Lists (`.csv`) alongside REAPER (`.rpp`) and Samplitude EDL (`v1.5`).
   [x] 1.2 Unselected Audio Timeline Handling (`export/daw.py`, `export/dialog.py`): Resolved timeline export exclusion of unselected audio gaps with configurable Exclude, Split, or Muted modes.
   [x] 1.3 Diagnostic Test Bench Expansion (`test_runner.py`): Added automated unit test (`DAW Timeline Interchange and Gap Handling`) verifying clip construction across all DAW export engines.

2. Current Release: WordPress Export Scope Routing & Filtering Fix (Completed in v3.5.6)
   [x] 2.1 Resolved WordPress Export Scope Regression (`plugins/wordpress/export_destination.py`): Fixed parent widget hierarchy lookup in `rebuild_post_items()` so `scope_combo.currentData()` is accurately retrieved from `UnifiedExportDialog` rather than defaulting to `"full"`.
   [x] 2.2 Synchronized Scope Signal Dispatch (`plugins/wordpress/export_destination.py`): Updated `on_scope_changed()` to explicitly pass the updated `scope` string parameter to `rebuild_post_items(scope=scope)` when users toggle dropdown options ("Selected Stories", "All Stories", "Full Episode", "Full Episode & All Stories").
   [x] 2.3 Diagnostic Test Bench Expansion (`test_runner.py`): Added automated unit test (`WordPress Export Scope Post Builder`) verifying post items rebuilding across all four export scope modes.

3. Upcoming Milestone: Google Docs & Drive Export Plugin (`plugins/gdocs`) [PLANNED for v3.6.0]
   [ ] 3.1 Plugin Architecture & OAuth 2.0 Authentication Engine (`plugins/gdocs/auth.py`, keyring token storage, local loopback handler)
   [ ] 3.2 Google Docs Document Serializer & REST Exporter (Formatted text, speaker headers, word highlights, story metadata)
   [ ] 3.3 Native Margin Comments & Text Range Anchoring (`drive.comments.create` integration for story notes & transcript comments)
   [ ] 3.4 Unified Export Center & Menu Integration (Progress worker, batch export, zero core bundle overhead)
   [ ] 3.5 (Optional) One-Time Review & Comment Pull ("Import Reviews from Google Doc" diff dialog)

4. Future Milestone: Video NLE & Avid Interchange [PLANNED for v3.7.0] [NOT STARTED]
   [ ] 4.1 Video NLE Interchange (FCPXML, Premiere / DaVinci Resolve xmeml XML)
   [ ] 4.2 Avid / Pro Tools AAF Composition
   [ ] 4.3 Rational Timecode & Audio Sample Rate Math

5. Future Milestone: Text-Driven Audio Editing & Precision Alignment (Phase 5) [NOT STARTED]
   [ ] 5.1 "Text-as-Timeline" Rough-Cut Editing Engine
   [ ] 5.2 Seamless Non-Destructive Playback Preview
   [ ] 5.3 CTC Word Boundary Refinement & Forced Alignment
   [ ] 5.4 Memory-Bounded Diarization & Chunked Streaming

================================================================================
CORE ARCHITECTURAL INVARIANTS
================================================================================
- Core modules must NEVER import from plugins/.
- Heavy translation runtimes must remain isolated inside plugins/translation/.
- Speaker diarization uses wespeakerruntime as the primary ONNX embedding engine.
- Real-time progress updates with percentage, status messages, and estimated time.
- Diagnostic logs written to OS standard logging locations.
- Offline-first architecture: core processing functions locally without internet.
- 0 MB Default GPU Footprint: Base installer contains 0 MB of CUDA bloat; GPU features are purely optional and provisioned on demand.

================================================================================
1. PREVIOUS RELEASE: PHASE 2 ARCHITECTURE MODULARIZATION (Completed in v3.5.0)
================================================================================

[x] 1.1 BACKGROUND WORKERS & WAVEFORM CACHE EXTRACTION (background_workers.py)
-------------------------------------------------------------------------------
- Extracted `StoryAutoDetectWorker`, `WaveformWorker`, `VideoThumbnailWorker`, and binary peak cache helpers (~984 lines) into `background_workers.py`.
- Preserved full concurrency guarantees, subprocess lifecycle locks, atomic peak cache file serialization (`RTVSPEAK`), and frame extraction density.

[x] 1.2 BATCH PROCESSING CENTER MODULARIZATION (batch_dialog.py)
-----------------------------------------------------------------
- Extracted `BatchProcessingDialog` and `BatchFileListWidget` drag-and-drop orchestrator (~507 lines) into `batch_dialog.py`.
- Maintained batch queueing, auto-detect profile presets, output folder resolution, and drag-and-drop file ingestion.

[x] 1.3 CUMULATIVE MONOLITH FOOTPRINT REDUCTION
------------------------------------------------
- Reduced monolithic `prs_shared.py` from historic 6,719 lines down to 1,082 lines (an 84% reduction) while maintaining 100% backward-compatible re-export shims across all internal callers and plugins.

[x] 1.4 STABLE RELEASE SNAPSHOT & GITHUB CHANNEL DEPLOYMENT
------------------------------------------------------------
- Tagged `v3.5.0-stable` on GitHub stable channel with snapshot archive in `.snapshots/v3.5.0-stable.tar.gz`.

================================================================================
2. CURRENT RELEASE: DAW TIMELINE INTERCHANGE & CI HARDENING (Completed in v3.5.2)
================================================================================

[x] 2.1 COCKOS REAPER PROJECT (.rpp) GENERATOR (export/daw.py)
--------------------------------------------------------------
- Implemented multi-track REAPER project generator (`generate_reaper_project`) producing compliant S-expressions (`<REAPER_PROJECT`, `<TRACK`, `<ITEM`, `<SOURCE WAV>`).
- Included source offset calculation (`SOFFS`), story titles (`NAME`), non-destructive fade-in/fade-out envelopes (`FADEIN`, `FADEOUT`), and color-coded project region markers (`MARKER`).

[x] 2.2 MAGIX SAMPLITUDE EDL (v1.5) EXPORT (export/daw.py)
----------------------------------------------------------
- Implemented Magix Samplitude and Sequoia compatible broadcast Edit Decision List v1.5 generator (`generate_samplitude_edl`).
- Formatted timestamps using millisecond-precision `HH:MM:SS:mmm` timecode math (`format_edl_timestamp`).

[x] 2.3 ONE-CLICK YOUTUBE CHAPTER MARKER CLIPBOARD COPY
--------------------------------------------------------
- Added direct clipboard integration (`copy_youtube_chapters_to_clipboard`) with global `Ctrl+Shift+Y` shortcut and dedicated button in `UnifiedExportDialog`.
- Automatically guarantees initial `00:00 - <First Story>` introduction marker for YouTube compatibility.

[x] 2.4 UNIFIED EXPORT CENTER & MENU INTEGRATION
-------------------------------------------------
- Added REAPER and Samplitude checkboxes in `UnifiedExportDialog` with persistent `QSettings` support.
- Added dedicated "Export Timeline / DAW" submenu in File menu and integrated with batch export pipeline.

[x] 2.5 DIAGNOSTIC TEST BENCH EXPANSION (test_runner.py)
--------------------------------------------------------
- Added automated diagnostic tests verifying REAPER project syntax, Samplitude EDL structure, and chapter marker zero-start formatting.

[x] 2.6 CI/CD PIPELINE HARDENING & ARTIFACT STAGING (.github/workflows/build.yml)
---------------------------------------------------------------------------------
- Isolated package staging in `dist/release_packages/` to bypass heavy unbundled PyInstaller build folders during artifact uploads.
- Added `continue-on-error: true` and direct GitHub Release asset publishing across Linux, Windows, macOS, and plugin build jobs.

================================================================================
3. UPCOMING MILESTONE: GOOGLE DOCS & DRIVE EXPORT PLUGIN [PLANNED for v3.6.0]
================================================================================

[ ] 3.1 Plugin Scaffolding & OAuth 2.0 Auth Engine (plugins/gdocs/auth.py)
[ ] 3.2 Google Docs Document Serializer & REST Exporter (plugins/gdocs/formatter.py, client.py)
[ ] 3.3 Native Margin Comments & Text Range Anchoring (plugins/gdocs/comments.py)
[ ] 3.4 Unified Export Center & Menu Integration (plugins/gdocs/plugin.py)
[ ] 3.5 One-Time Review & Comment Pull Integration (plugins/gdocs/review.py)

================================================================================
4. FUTURE MILESTONE: EXPANDED NLE & DAW TIMELINE INTERCHANGE [PLANNED for v3.7]
================================================================================

[ ] 4.1 Cockos Reaper Project (.rpp) Generator
[ ] 4.2 Magix Samplitude EDL (v1.5) Export
[ ] 4.3 Video NLE Interchange (FCPXML, Premiere / DaVinci Resolve xmeml XML)
[ ] 4.4 Avid / Pro Tools AAF Composition
[ ] 4.5 Rational Timecode & Audio Sample Rate Math
[ ] 4.6 UI & Batch Export Integration

================================================================================
5. FUTURE MILESTONE: TEXT-DRIVEN AUDIO EDITING & PRECISION ALIGNMENT [PLANNED for v4.0]
================================================================================

[ ] 5.1 "Text-as-Timeline" Rough-Cut Editing Engine
[ ] 5.2 Seamless Non-Destructive Playback Preview
[ ] 5.3 CTC Word Boundary Refinement & Forced Alignment
[ ] 5.4 Memory-Bounded Diarization & Chunked Streaming
