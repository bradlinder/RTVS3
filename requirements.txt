# Radio & TV Segmenter 1.1 -- base (CPU-only) runtime dependencies.
#
# This app is CPU-only by default; GPU acceleration is a separate, optional
# download from Settings > Processing > GPU Acceleration (see
# runtime_manager.py's "gpu_transcribe" environment and gpu_acceleration.py).
# For that reason, install torch from the CPU wheel index rather than plain
# PyPI -- on Windows and Linux, PyPI's default torch wheel bundles CUDA
# runtime libraries and is well over a gigabyte larger than the CPU build:
#
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
#
# (On macOS this doesn't matter -- PyPI only ships CPU/MPS wheels there.)
# build_installer.py checks for this and warns if it detects a CUDA-enabled
# torch in the build environment.

PySide6>=6.7,<7
python-docx>=1.1,<2
faster-whisper>=1.1,<2
ctranslate2>=4.0.0,<5
diarize==0.1.2
silero-vad>=5,<7
huggingface-hub>=0.23,<2
pypdf>=4,<7
requests>=2.28.1,<3
numpy>=1.26.0,<2.0.0
keyring>=25,<26
torch>=2.0,<2.4
torchaudio>=2.0,<2.4
soundfile>=0.12.1

pywin32-ctypes>=0.2.2; sys_platform == 'win32'
pywinstyles>=1.8; sys_platform == 'win32'

cryptography>=42,<46
psutil>=5.9,<7
onnxruntime>=1.17,<2
sherpa-onnx>=1.13,<2
scipy>=1.11.0,<1.15.0
scikit-learn>=1.4.0,<1.6.0
# Speaker diarization ONNX voice embedding engine (WeSpeaker ResNet34-LM)
wespeakerruntime>=1.0.0,<2.0.0

